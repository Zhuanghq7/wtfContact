<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.NoTitleBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/Animation_Activity</item>
    </style>
    <style name="Animation_Activity" parent="@android:style/Animation.Translucent">
        <item name="android:windowEnterAnimation">@anim/act_in</item>
        <item name="android:windowExitAnimation">@anim/act_out</item>
        <item name="android:activityOpenEnterAnimation">@anim/act_in</item>
        <item name="android:activityOpenExitAnimation">@anim/act_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/act_in</item>
        <item name="android:activityCloseExitAnimation">@anim/act_out</item>
    </style>
</resources>
